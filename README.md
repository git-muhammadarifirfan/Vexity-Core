# VexityCore

Landing page + portfolio (Next.js App Router + Tailwind).

## Run (pnpm)

```bash
corepack enable
pnpm install
pnpm dev
```

Open: http://localhost:3000

## Scripts

- `pnpm dev` – development server
- `pnpm build` – production build
- `pnpm start` – start production server

